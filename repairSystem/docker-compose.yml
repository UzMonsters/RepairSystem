services:
  frontend:
    build: .
    container_name: repair-system-frontend
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      NUXT_BACKEND_URL: http://host.docker.internal:8080
