spring.application.name=repair-auto
server.port=${PORT:${SERVER_PORT:8080}}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9pcrvnlk1mc73drfecg-a.oregon-postgres.render.com:5432/repair_e3z2}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AbNhggb1FMDZkw88xZYlUm41uocGbEGe}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true
management.endpoint.health.show-details=never
management.info.env.enabled=true
info.app.name=RepairAuto
info.app.phase=12

spring.jackson.time-zone=UTC
spring.mvc.problemdetails.enabled=false
spring.web.resources.add-mappings=false
spring.mvc.throw-exception-if-no-handler-found=true

springdoc.api-docs.enabled=${APP_OPENAPI_ENABLED:false}
springdoc.swagger-ui.enabled=${APP_SWAGGER_UI_ENABLED:false}
springdoc.swagger-ui.path=/swagger-ui.html

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:}
app.cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
app.cors.allowed-headers=Authorization,Content-Type,X-Trace-Id
app.cors.exposed-headers=X-Trace-Id
app.cors.allow-credentials=false
app.trace.header-name=X-Trace-Id
logging.pattern.level=%5p [traceId:%X{traceId:-}]

app.business-time-zone=${APP_BUSINESS_TIME_ZONE:Asia/Tashkent}

app.jwt.secret=${APP_JWT_SECRET:local-only-jwt-secret-that-is-long-enough-for-development-use}
app.jwt.issuer=${APP_JWT_ISSUER:repair-auto}
app.jwt.access-token-ttl=${APP_JWT_ACCESS_TOKEN_TTL:PT15M}
app.auth-throttle.enabled=${APP_AUTH_THROTTLE_ENABLED:true}
app.auth-throttle.max-failures=${APP_AUTH_THROTTLE_MAX_FAILURES:10}
app.auth-throttle.window=${APP_AUTH_THROTTLE_WINDOW:PT10M}
app.auth-throttle.block-duration=${APP_AUTH_THROTTLE_BLOCK_DURATION:PT15M}
app.auth-throttle.retention=${APP_AUTH_THROTTLE_RETENTION:P1D}
app.refresh-token-ttl=${APP_REFRESH_TOKEN_TTL:P1D}
app.remember-me-refresh-token-ttl=${APP_REMEMBER_ME_REFRESH_TOKEN_TTL:P30D}
app.mobile-refresh-token-ttl=${APP_MOBILE_REFRESH_TOKEN_TTL:P30D}
app.bootstrap-admin.email=${APP_BOOTSTRAP_ADMIN_EMAIL:}
app.bootstrap-admin.password=${APP_BOOTSTRAP_ADMIN_PASSWORD:}
app.bootstrap-admin.full-name=${APP_BOOTSTRAP_ADMIN_FULL_NAME:System Administrator}
app.bootstrap-admin.enabled=${APP_BOOTSTRAP_ADMIN_ENABLED:false}

spring.servlet.multipart.max-file-size=${APP_STORAGE_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${APP_STORAGE_MAX_REQUEST_SIZE:12MB}
spring.servlet.multipart.file-size-threshold=2MB

app.storage.enabled=${APP_STORAGE_ENABLED:true}
app.storage.endpoint=${APP_STORAGE_ENDPOINT:http://localhost:9000}
app.storage.region=${APP_STORAGE_REGION:us-east-1}
app.storage.bucket=${APP_STORAGE_BUCKET:repairauto-local}
app.storage.access-key=${APP_STORAGE_ACCESS_KEY:minioadmin}
app.storage.secret-key=${APP_STORAGE_SECRET_KEY:minioadmin}
app.storage.path-style=${APP_STORAGE_PATH_STYLE:true}
app.storage.create-bucket=${APP_STORAGE_CREATE_BUCKET:false}
app.storage.download-url-ttl=${APP_STORAGE_DOWNLOAD_URL_TTL:PT10M}
app.storage.max-file-size=${APP_STORAGE_MAX_FILE_SIZE:10MB}
app.storage.max-files-per-request=${APP_STORAGE_MAX_FILES_PER_REQUEST:30}
app.storage.max-files-per-type=${APP_STORAGE_MAX_FILES_PER_TYPE:10}

app.telegram.enabled=${APP_TELEGRAM_ENABLED:false}
app.telegram.customer.bot-token=${APP_TELEGRAM_CUSTOMER_BOT_TOKEN:${APP_TELEGRAM_BOT_TOKEN:8957154846:AAFmOeFl2017WuP3Rh1kXWmxMjdj7GsONCc}}
app.telegram.customer.webhook-secret=${APP_TELEGRAM_CUSTOMER_WEBHOOK_SECRET:${APP_TELEGRAM_WEBHOOK_SECRET:omCWeMfbbEAnWWBXV4Wb_IiI1OdrEqMcme8Y0YpglEtVAtkb}}
app.telegram.customer.bot-username=${APP_TELEGRAM_CUSTOMER_BOT_USERNAME:${APP_TELEGRAM_BOT_USERNAME:RepairAutoCustomerTestBot}}
app.telegram.technician.bot-token=${APP_TELEGRAM_TECHNICIAN_BOT_TOKEN:8854105729:AAFJDiIBRJm0RY6CyCtlYHH_IG0jwZcvOkg}
app.telegram.technician.webhook-secret=${APP_TELEGRAM_TECHNICIAN_WEBHOOK_SECRET:_fe0-fFvJlQMFLOX5P4bnIsr-NYIgJ9gRHYfWbPLwRTpQv7I}
app.telegram.technician.bot-username=${APP_TELEGRAM_TECHNICIAN_BOT_USERNAME:RepairAutoStaffTestBot}
app.telegram.api-base-url=${APP_TELEGRAM_API_BASE_URL:https://api.telegram.org}
app.telegram.file-base-url=${APP_TELEGRAM_FILE_BASE_URL:https://api.telegram.org}
app.telegram.request-timeout=${APP_TELEGRAM_REQUEST_TIMEOUT:PT10S}
app.telegram.max-pending-photos=${APP_TELEGRAM_MAX_PENDING_PHOTOS:3}
app.telegram.max-webhook-body-size=262144

app.telegram-login.issuer=${APP_TELEGRAM_LOGIN_ISSUER:https://oauth.telegram.org}
app.telegram-login.jwks-uri=${APP_TELEGRAM_LOGIN_JWKS_URI:https://oauth.telegram.org/.well-known/jwks.json}
app.telegram-login.customer.client-id=${APP_TELEGRAM_CUSTOMER_LOGIN_CLIENT_ID:${TELEGRAM_CUSTOMER_LOGIN_CLIENT_ID:}}
app.telegram-login.technician.client-id=${APP_TELEGRAM_TECHNICIAN_LOGIN_CLIENT_ID:${TELEGRAM_TECHNICIAN_LOGIN_CLIENT_ID:}}
app.telegram-login.allowed-clock-skew=${APP_TELEGRAM_LOGIN_CLOCK_SKEW:PT60S}

app.notification.worker-enabled=${APP_NOTIFICATION_WORKER_ENABLED:true}
app.notification.poll-interval=${APP_NOTIFICATION_POLL_INTERVAL:PT5S}
app.notification.batch-size=${APP_NOTIFICATION_BATCH_SIZE:50}
app.notification.processing-lease=${APP_NOTIFICATION_PROCESSING_LEASE:PT2M}
app.notification.max-attempts=${APP_NOTIFICATION_MAX_ATTEMPTS:8}
app.notification.initial-backoff=${APP_NOTIFICATION_INITIAL_BACKOFF:PT10S}
app.notification.max-backoff=${APP_NOTIFICATION_MAX_BACKOFF:PT30M}

app.firebase.enabled=${APP_FIREBASE_ENABLED:false}
app.firebase.project-id=${APP_FIREBASE_PROJECT_ID:repairauto-dev}
app.firebase.credentials-path=${APP_FIREBASE_CREDENTIALS_PATH:}
app.firebase.connect-timeout=${APP_FIREBASE_CONNECT_TIMEOUT:PT10S}
app.firebase.read-timeout=${APP_FIREBASE_READ_TIMEOUT:PT10S}
app.firebase.endpoint-stale-after=${APP_FIREBASE_ENDPOINT_STALE_AFTER:P90D}
app.firebase.stale-cleanup-enabled=${APP_FIREBASE_STALE_CLEANUP_ENABLED:false}
app.firebase.stale-cleanup-interval=${APP_FIREBASE_STALE_CLEANUP_INTERVAL:P1D}

app.dashboard.business-time-zone=${APP_DASHBOARD_BUSINESS_TIME_ZONE:${APP_BUSINESS_TIME_ZONE:Asia/Tashkent}}

app.cleanup.enabled=${APP_CLEANUP_ENABLED:false}
app.cleanup.batch-size=${APP_CLEANUP_BATCH_SIZE:100}
app.cleanup.interval=${APP_CLEANUP_INTERVAL:PT15M}
app.cleanup.stale-upload-threshold=${APP_CLEANUP_STALE_UPLOAD_THRESHOLD:PT1H}
app.cleanup.deleted-object-retention=${APP_CLEANUP_DELETED_OBJECT_RETENTION:P7D}
app.cleanup.refresh-session-retention=${APP_CLEANUP_REFRESH_SESSION_RETENTION:P30D}
app.cleanup.telegram-update-retention=${APP_CLEANUP_TELEGRAM_UPDATE_RETENTION:P30D}
app.cleanup.notification-attempt-retention=${APP_CLEANUP_NOTIFICATION_ATTEMPT_RETENTION:P30D}

server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=${APP_SHUTDOWN_TIMEOUT:30s}
server.forward-headers-strategy=framework
server.max-http-request-header-size=${APP_MAX_HTTP_REQUEST_HEADER_SIZE:16KB}

spring.flyway.validate-on-migrate=true
spring.flyway.clean-disabled=true
spring.flyway.out-of-order=false
app.flyway.repair-on-startup=${APP_FLYWAY_REPAIR_ON_STARTUP:false}

spring.datasource.hikari.maximum-pool-size=${APP_DB_POOL_MAX_SIZE:10}
spring.datasource.hikari.minimum-idle=${APP_DB_POOL_MIN_IDLE:2}
spring.datasource.hikari.connection-timeout=${APP_DB_POOL_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.validation-timeout=${APP_DB_POOL_VALIDATION_TIMEOUT:5000}
spring.datasource.hikari.idle-timeout=${APP_DB_POOL_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${APP_DB_POOL_MAX_LIFETIME:1800000}
spring.datasource.hikari.leak-detection-threshold=${APP_DB_POOL_LEAK_DETECTION_THRESHOLD:0}

management.endpoint.health.group.liveness.include=livenessState
management.endpoint.health.group.readiness.include=readinessState,db,diskSpace
